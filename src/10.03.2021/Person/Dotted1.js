import classes from "./Dotted1.module.css";
const Dotted1 = ({children}) => {
    return ( <div>
        <button className={classes.Dotted1}>{children}</button>
    </div> );
}
export default Dotted1;