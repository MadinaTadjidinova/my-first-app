import classes from "./Form.module.js"

const Form = ({children}) => {
    return(
        <div>
            <form>
                <h2>Login</h2>
            </form>
        </div>
    )
}

export default Form;